{
  "name": "proxy",
  "version": "0.0.1",
  "description": "proxy-server",
  "scripts": {
    "proxy": "node --env-file .env ./server.cjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/http-proxy": "^10.0.1",
    "@fastify/static": "^8.0.2",
    "fastify": "^5.0.0"
  }
}
